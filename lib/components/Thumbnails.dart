import 'package:du_an_animal/Models/GalleryItemModel.dart';
import 'package:flutter/material.dart';

class MyThumbNails extends StatelessWidget {
  final GalleryItemModel itemModel;
  final GestureTapCallback ontap;
  const MyThumbNails({super.key, required this.itemModel, required this.ontap});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
